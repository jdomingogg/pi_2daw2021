export class Juego {
  constructor(public id: number, public nombre: string, public descripcion: string, public precio: number, public stock: number, public imagen: string, public id_categoria: number){

  }
}
